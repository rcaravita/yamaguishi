$("#cart").html("<%= escape_javascript(render partial: 'checkout') %>");
$(document).ready(function () {
	$([document.documentElement, document.body]).animate({
		scrollTop: $("#main").offset().top
	}, 1000);
});
